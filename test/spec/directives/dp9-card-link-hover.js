'use strict';

describe('Directive: dp9CardLinkHover', function () {

  // load the directive's module
  beforeEach(module('deepspace9App'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    //scope = $rootScope.$new();
  }));

  //it('should make hidden element visible', inject(function ($compile) {
  //  element = angular.element('<dp9-card-link-hover></dp9-card-link-hover>');
  //  element = $compile(element)(scope);
  //  expect(element.text()).toBe('this is the dp9CardLinkHover directive');
  //}));
});
