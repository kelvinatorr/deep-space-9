'use strict';

describe('Service: Positions', function () {

    // load the service's module
    beforeEach(module('deepspace9App'));

    // instantiate service
    var Positions;
    beforeEach(inject(function (_Positions_) {
        Positions = _Positions_;
    }));

    //it('should do something', function () {
    //    expect(!!positions).toBe(true);
    //});

});
