'use strict';

describe('Service: PositionDetail', function () {

  // load the service's module
  beforeEach(module('deepspace9App'));

  // instantiate service
  var PositionDetail;
  beforeEach(inject(function (_PositionDetail_) {
      PositionDetail = _PositionDetail_;
  }));

  //it('should do something', function () {
  //  expect(!!PositionDetail).toBe(true);
  //});

});
