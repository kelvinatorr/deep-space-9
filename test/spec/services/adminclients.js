'use strict';

describe('Service: AdminClients', function () {

  // load the service's module
  beforeEach(module('deepspace9App'));

  // instantiate service
  var AdminClients;
  beforeEach(inject(function (_AdminClients_) {
      AdminClients = _AdminClients_;
  }));

});
